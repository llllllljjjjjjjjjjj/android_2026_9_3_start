# -*- coding: utf-8 -*-
"""重写 templates.md §2.5 —— 匹配工作流 D 的新定位（访问流程复刻 / 本地模拟）"""
import re

P = r"D:\reserve_agent\android\.dsh\skills\risk-control-adversary\references\templates.md"
t = open(P, encoding="utf-8").read()
open(P + ".bak-25", "w", encoding="utf-8").write(t)

i = t.find("## §2.5")
j = t.find("## §2 项目风控记录模板")
assert 0 < i < j, "边界定位失败"

NEW = r"""## §2.5 单接口访问流程复刻模板（SKILL.md 工作流 D）

写入路径：`projects/<target>/docs/flow-and-risk.md`

**用途**：把真实 App 访问某接口的「前 / 中 / 后」完整记录下来，产出**可在本地照着重演的访问规格**，
用于在本地实现该接口访问而**不触发风控**。

> 验收标准：**别人拿着这份文档能独立把本地访问实现出来，无需回头问"这里该发什么"。**

```markdown
# <目标> 接口访问流程复刻规格

> 生成日期: YYYY-MM-DD | 平台: | 涉及接口: IF-xx… | 目标：本地复刻该访问流程
> 资料基线: <抓包/hook 证据路径>

## 一、完整时序（前 / 中 / 后 三段，两轮复现对照）

| 段 | 序 | 接口 | 方法 | Host | 轮次1 | 轮次2 | 稳定性 |
|---|---|---|---|---|---|---|---|
| **前** | 1 | /api/前置 | POST | ... | ✓ | ✓ | 恒定 |
| **中** | 2 | /api/目标 | POST | ... | ✓ | ✓ | 恒定 |
| **后** | 3 | /api/上报 | POST | ... | ✓ | ✓ | 恒定 |

> 稳定性：恒定 / 可变 / 偶发。**顺序即规格**，不能只列集合；单轮数据不足以判定。

## 二、逐笔请求的可复刻参数快照

### step 1 — <前置接口>
- endpoint / 方法 / Content-Type：
- 参数清单（标注来源）：
  | 参数 | 值/形态 | 来源（本地生成 / 上一笔产出 / 服务端签发） |
  |---|---|---|
- 请求头（含签名类，注明是否 native 注入）：
- body 形态（明文/加密）：

### step 2 — <目标接口>
（同上结构）

### step 3 — <配套上报>
（同上结构）

## 三、★ 数据依赖链（决定本地发请求的顺序）

```
本地生成 ─┐
          ├─→ step1 <前置>  响应字段 xxx ─┐
          │                                ├─→ step3 <目标>
服务端签发 xxx ───────────────────────────┘
                                    ↓
                              step4 <配套上报>（同批）
```

> 有依赖的步骤**必须串行**，不能跳步、不能并发。

## 四、前置埋点 / 特征上报链路（D3 六问全答）

**4.1 前置接口**
| 接口 | 作用 | 出现轮次 | 必须性 | 产出被谁用 | 证据 |
|---|---|---|---|---|---|

**4.2 紧邻上报**
| 上报接口 | 与哪笔配对 | 延迟量级 | 配对强度 | 证据 |
|---|---|---|---|---|

**4.3 上报内容**
| 上报接口 | 载荷量级 | 事件数 | 可读性 | 关键字段 | 加密归属 |
|---|---|---|---|---|---|

**4.4 上报通道**
| 通道 | 接口 | 职责 | 是否含指纹 |
|---|---|---|---|

## 五、★ 本地复刻规格

### 5.1 访问序列（照抄执行顺序）
```
step 1: <接口A>    参数来源: 本地生成        | 依赖: -
step 2: <接口B>    参数来源: A 响应字段 xxx  | 依赖: step1
step 3: <目标接口>  参数来源: 本地生成 + step1 的 xxx + 服务端签发 xxx
step 4: <配套上报>  参数来源: 本地生成        | 触发时机: 目标请求完成后立即
```

### 5.2 每笔复刻要点表
| step | 接口 | 关键参数 | 参数来源 | 必须的凭证 | 头/签名要求 | 可否省略 | 省略后果 |
|---|---|---|---|---|---|---|---|

### 5.3 节奏与顺序要求
- 各步间隔量级（实测值）：
- 必须严格串行的步骤：
- 上报是否须落在同一会话窗口：

### 5.4 本地实现检查清单（做完逐条打勾）
- [ ] 前置接口都发了，且其产出被正确引用
- [ ] 目标请求携带全部必填参数与凭证（含服务端签发的）
- [ ] 配套上报在目标请求后同批发出
- [ ] 参数完整性：可选参数也带上（"只带必要参数"本身即脚本指纹）
- [ ] 头清单与真实 App 一致（含 native 注入的签名类）
- [ ] 节奏符合实测间隔

## 六、配对关系表（三段联动）

| 目标接口 | 前置（必须先发生） | 前置产出被谁用 | 紧邻（必须同时发生） | 回执（必须回带） | 配对强度 | 证据 |
|---|---|---|---|---|---|---|

## 七、降级判据（本地自检用）

| 级别 | 特征 | 判据 | 处置 |
|---|---|---|---|
| OK | 含业务字段 | | 继续 |
| 软降级 | 空壳（HTTP200 + 业务码正常但列表空） | | 熔断 + 归因 |
| 硬拒绝 | 风控码（如 -99999） | | 立即停止 |

> 三级均不得仅凭 HTTP 200 或业务 code 判成功。

## 八、待验证实验清单

| 编号 | 假设 | 实验设计（阻断实验） | 优先级 |
|---|---|---|---|
| V-1 | 缺 <某笔> 致降级 | 阻断该笔，观察目标响应 | 高 |
```

"""

t2 = t[:i] + NEW + t[j:]
open(P, "w", encoding="utf-8").write(t2)

chk = open(P, encoding="utf-8").read()
print("§2.5 存在:", "## §2.5" in chk)
print("标题更新:", "单接口访问流程复刻模板" in chk)
print("含数据依赖链:", "数据依赖链" in chk)
print("含本地复刻规格:", "本地复刻规格" in chk)
print("含检查清单:", "本地实现检查清单" in chk)
print("含前/中/后:", "前 / 中 / 后" in chk)
print("templates.md 行数:", chk.count("\n") + 1)
