# -*- coding: utf-8 -*-
"""干净重写工作流 D（只替换 D 区块，不碰其他）"""
import re

P = r"D:\reserve_agent\android\.dsh\skills\risk-control-adversary\SKILL.md"
t = open(P, encoding="utf-8").read()

# 结构检查
heads = [(m.start(), m.group(1)) for m in re.finditer(r"^## 工作流 ([A-Z])：", t, re.M)]
assert [h[1] for h in heads] == ["A", "B", "C", "D"], f"结构异常: {[h[1] for h in heads]}"

iD = t.find("## 工作流 D")
iExt = t.find("## 扩展协议")
assert 0 < iD < iExt, "D/扩展协议 边界定位失败"

NEW_D = r"""## 工作流 D：单接口访问流程复刻（本地模拟真实 App 的完整访问）

触发：用户说「分析 <接口> 的整体流程」「<接口> 是怎么走的」「注意风控点与前置埋点上报链路」
「<关键词搜索/评论/视频> 接口的实现」「分析这条链路的埋点」「本地模拟 <接口> 的访问」

**目的（先明确，否则会写偏）**：把**真实 App 访问某接口时「前 / 中 / 后」分别发生什么**完整记录下来，
产出一份**可在本地照着重演的访问规格**——用它驱动本地实现，让本地访问在风控看来与真实 App 无异，
**从而不触发风控**。

> 本工作流**不是**写一份"分析报告"。判定标准：产出的东西**能不能直接拿去实现本地访问**？
> 若只说了"有哪些风控点"，却说不清"先发什么、带什么、再发什么、回什么、之后补什么"——就是没做完。

**D1. 完整时序记录（前 / 中 / 后）。**

逐笔记录真实 App 的一次完整访问，分三段：

| 段 | 记什么 | 为什么必须 |
|---|---|---|
| **前** | 目标接口之前，App 先访问了哪些接口？各自带什么参数？ | 前置接口常产出目标接口**必需**的参数/凭证，缺了必失败 |
| **中** | 目标接口完整请求（URL/方法/全部参数/body/头）+ 完整响应 | 主体 |
| **后** | 目标接口之后，App 立刻又访问了哪些接口？（上报/统计/预取） | 配套上报是**风控配对**的另一半，缺了会降级 |

**要求**：两轮同操作对照，标每笔稳定性（恒定/可变/偶发）；**顺序即规格**，不能只列集合。
hook 位置：Android `com.bytedance.retrofit2.RequestBuilder.build` + 响应解析入口；
Web 用网络面板按 initiator 链。

**D2. 逐笔请求的可复刻参数快照。**

对三段中**每一笔**请求，记录足以在本地复现的信息：

- 完整 endpoint（host + path）、方法、Content-Type
- **全部参数/字段名**（业务 + 公共 + 行为），并标注每个参数的来源：
  **本地可生成** / **上一笔的产出** / **服务端签发（必须回带）**
- 请求头清单（尤其签名类——可能由 native 层注入，Java 层看不到，见 E-31）
- body 形态（明文/加密；加密则转 protocol-signature-reverser）

> **数据依赖链是重点**：把「A 接口的响应 → 成为 B 接口的入参」显式画出来。
> 这条链决定本地实现必须按什么顺序发请求，**不能跳步**。

**D3. 前置埋点 / 特征上报链路（强制，逐一回答）。**

目标接口能不能用，往往不取决于它本身，而取决于**配套请求有没有一起发生**（E-28）。
**必须逐项回答，任一项无证据即列入待验证清单，不得跳过**：

**D3.1 前置接口识别**——目标接口**之前**有哪些必须发生的请求？
- 典型：历史记录上报、页面曝光、前置详情、预热/预取、鉴权换票
- 判据：同操作**两轮复现**中恒定出现；并验证其产出是否被目标接口引用（数据依赖）

**D3.2 紧邻上报识别**——目标接口**之后立即**（同批/数秒内）发了什么？
- 典型：特征上报（`upload_ei_feature`）、批量埋点（`app_log`）、行为统计（`aweme/stats`）、监控
- 判据：与目标请求**成对出现**且共享会话时间窗

**D3.3 上报内容解析**——装的是什么、能不能读？
- 先查可读性：包头是否含明文密钥/IV、事件参数是否明文 → 可读则提取字段语义
- 不可读则记加密位置与归属，转 protocol-signature-reverser

**D3.4 上报通道梳理**——几个通道、各自职责、载荷量级、是否含设备指纹。

**D3.5 配对关系成表**（模板见 D5）。

**D3.6 因果验证**——对「缺配套请求 → 降级/拒绝」做**阻断实验**（见 D7）。

**D4. ★ 本地复刻规格（本工作流的交付物）。**

把 D1–D3 转成**可直接实现的规格**：

**D4.1 访问序列（照抄执行顺序）**

```
step 1: <接口A>    参数来源: 本地生成        | 依赖: -
step 2: <接口B>    参数来源: A 响应字段 xxx  | 依赖: step1
step 3: <目标接口>  参数来源: 本地生成 + step1 的 xxx + 服务端签发 xxx
step 4: <配套上报>  参数来源: 本地生成        | 触发时机: 目标请求完成后立即
```

**D4.2 每笔的复刻要点表**

| step | 接口 | 关键参数 | 参数来源 | 必须的凭证 | 头/签名要求 | 可否省略 | 省略后果 |
|---|---|---|---|---|---|---|---|

**D4.3 节奏与顺序要求**：各步间隔量级（实测值）；哪些必须严格串行（有数据依赖的）；
上报是否必须落在同一会话窗口。

**D4.4 本地实现检查清单**（做完逐条打勾）
- [ ] 前置接口是否都发了，且其产出被正确引用
- [ ] 目标请求是否携带全部必填参数与凭证（含服务端签发的）
- [ ] 配套上报是否在目标请求后同批发出
- [ ] 参数完整性：可选参数是否也带上（"只带必要参数"本身即脚本指纹）
- [ ] 头清单是否与真实 App 一致（含 native 注入的签名类）
- [ ] 节奏是否符合实测间隔

**D5. 配对关系表（前 / 中 / 后 三段联动）。**

| 目标接口 | 前置（必须先发生） | 前置产出被谁用 | 紧邻（必须同时发生） | 回执（必须回带） | 配对强度 | 证据 |
|---|---|---|---|---|---|---|

**配对强度判据**：两轮复现均恒定成对 = 强；单轮出现或顺序可变 = 中；仅偶发 = 弱。

**D6. 降级判据（用于本地自检）。**

给出三级判据，本地实现必须内置：真数据 / 软降级空壳 / 硬拒绝风控码（E-30）。
任何一级都不得仅凭 HTTP 200 或业务 code 判定成功（AGENTS.md 禁词纪律）。

**D7. 因果验证（强制，区别于相关性观测）。**

凡断言「A 导致 B」（如「缺埋点导致降级」「缺签名导致拒绝」），**必须设计阻断实验**：
人为阻断 A（不发某笔 / 去掉某凭证），观测 B 是否出现。
**仅有时序相关而无阻断实验的结论，证据等级只能标「推测」**。
实验设计写入产出文档的「待验证」章节。

**D8. 产出与沉淀。**
- 复刻规格文档 → `projects/<target>/docs/flow-and-risk.md`，**必含**：
  三段时序表（前/中/后）/ 逐笔参数快照 / **数据依赖链** / D3 六问全答 /
  **D4 本地复刻规格（序列 + 要点表 + 检查清单）** / 配对关系表 / 降级判据 / 待验证实验清单
- 接口维度登记 → 更新 `references/projects/<target>.md` 的「接口风控速查表」
- 可泛化角度 → 按 A5 沉淀 L1

**产出验收标准**：**别人拿着这份文档能独立把本地访问实现出来，无需回头问"这里该发什么"。**

"""

t2 = t[:iD] + NEW_D + t[iExt:]
open(P, "w", encoding="utf-8").write(t2)

# ---- 严格校验 ----
chk = open(P, encoding="utf-8").read()
heads2 = [(m.start(), m.group(1)) for m in re.finditer(r"^## 工作流 ([A-Z])：", chk, re.M)]
print("工作流标题:", [h[1] for h in heads2])
print("无重复:", [h[1] for h in heads2] == ["A", "B", "C", "D"])

d = chk[chk.find("## 工作流 D"):chk.find("## 扩展协议")]
ids = re.findall(r"\*\*(D\d)\.", d)
print("D 内编号:", ids)
print("编号无重复:", len(ids) == len(set(ids)))
print("含 本地复刻规格:", "本地复刻规格" in d)
print("含 验收标准:", "产出验收标准" in d)
print("含 数据依赖链:", "数据依赖链" in d)
print("含 前/中/后:", ("前 / 中 / 后" in d) or ("前 / 中 / 后" in d))
print("SKILL.md 行数:", chk.count("\n") + 1)
