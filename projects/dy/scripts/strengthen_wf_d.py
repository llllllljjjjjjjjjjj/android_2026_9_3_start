# -*- coding: utf-8 -*-
"""补强工作流 D 的逻辑严谨性
    1) D1 加稳定性判据 + 两轮不一致处理
    2) D2 参数来源加「未知」第四类
    3) 各步产出表加 证据等级/置信度 列
    4) D6 降级判据须有对照实测证据
    5) 新增 D0：证据来源优先级（继承 A1.5）
    6) 同步 templates.md §2.5
"""
import re

SK = r"D:\reserve_agent\android\.dsh\skills\risk-control-adversary\SKILL.md"
TP = r"D:\reserve_agent\android\.dsh\skills\risk-control-adversary\references\templates.md"

for f in (SK, TP):
    open(f + ".bak-ev", "w", encoding="utf-8").write(open(f, encoding="utf-8").read())

# ============ A. SKILL.md ============
t = open(SK, encoding="utf-8").read()
iD = t.find("## 工作流 D")
iExt = t.find("## 扩展协议")
assert 0 < iD < iExt

NEW_D = r"""## 工作流 D：单接口访问流程复刻（本地模拟真实 App 的完整访问）

触发：用户说「分析 <接口> 的整体流程」「<接口> 是怎么走的」「注意风控点与前置埋点上报链路」
「<关键词搜索/评论/视频> 接口的实现」「分析这条链路的埋点」「本地模拟 <接口> 的访问」

**目的（先明确，否则会写偏）**：把**真实 App 访问某接口时「前 / 中 / 后」分别发生什么**完整记录下来，
产出一份**可在本地照着重演的访问规格**——用它驱动本地实现，让本地访问在风控看来与真实 App 无异，
**从而不触发风控**。

> 本工作流**不是**写一份"分析报告"。判定标准：产出的东西**能不能直接拿去实现本地访问**？
> 若只说了"有哪些风控点"，却说不清"先发什么、带什么、再发什么、回什么、之后补什么"——就是没做完。
>
> **适用范围**：本工作流服务**离线纯协议实现**（自己发请求）。若走 RPC 桥接（App 发请求），
> 前置/上报/凭证均由 App 保障，只需取本工作流的**降级判据 + 字段谱系**两块，不要套用全流程校验。

**D0. 证据纪律（全程适用，先立规矩）。**

**D0.1 证据来源优先级**（继承 A1.5，冲突时取高优先）：
`官方文档/协议规范` ＞ `本项目实测抓包/hook` ＞ `技术社区` ＞ `自媒体`。
本工作流的证据**默认应为实测**；用了低优先级来源必须在产出中标注来源类型。

**D0.2 每个结论都要带证据等级与置信度**（阈值见 templates.md §3.1）：
- **证据等级**：已实证 / 大样本 / 小样本 / 推测 / 未知
- **置信度**：高 / 中 / 低
- 两轴正交，**互不推导**；样本量按 `<账号数>账号 × <IP/设备数> × <复现次数>` 记录
- **证据不足写「未知」，禁止用推测填空充数**

**D0.3 因果论断的证据门槛**：凡断言「A 导致 B」，**必须有阻断实验**；仅有观测相关性的，
**证据等级最高只能标「推测」**（详见 D7）。

**D0.4 每一笔记录都要有原始证据路径**（文件 + 位置），不允许只写结论。

**D1. 完整时序记录（前 / 中 / 后）。**

逐笔记录真实 App 的一次完整访问，分三段：

| 段 | 记什么 | 为什么必须 |
|---|---|---|
| **前** | 目标接口之前，App 先访问了哪些接口？各自带什么参数？ | 前置接口常产出目标接口**必需**的参数/凭证，缺了必失败 |
| **中** | 目标接口完整请求（URL/方法/全部参数/body/头）+ 完整响应 | 主体 |
| **后** | 目标接口之后，App 立刻又访问了哪些接口？（上报/统计/预取） | 配套上报是**风控配对**的另一半，缺了会降级 |

**记录要求**：**顺序即规格**，不能只列集合。hook 位置：Android
`com.bytedance.retrofit2.RequestBuilder.build` + 响应解析入口；Web 用网络面板按 initiator 链。

**D1.1 稳定性判据（复现 ≥2 轮同操作）**

| 稳定性 | 判定标准 | 写入规格的方式 |
|---|---|---|
| **恒定** | ≥2 轮均出现，且相对位置一致 | 作为**必发步骤**写入 D4 访问序列 |
| **可变** | ≥2 轮均出现，但顺序/位置不固定 | 写入序列但标注"顺序可变" |
| **偶发** | 仅部分轮次出现 | 记录在案，**不进主序列**（备注触发条件） |

**D1.2 轮次不一致时的处理**（禁止含糊）

- 两轮结果**冲突**（如 A 轮有、B 轮无）→ **追加第 3 轮**；仍不稳定则标「偶发」+ 注明条件
- 某笔**无法判定**归属（是前置还是后置）→ 标 `未知` 并列入 D8 待验证清单，**不得猜**
- **只跑了 1 轮** → 全部标「小样本」，「恒定」判定不成立，D8 必须列"补跑第 2 轮"

**D1.3 D1 产出表**（须带证据字段）

| 段 | 序 | 接口 | 方法 | Host | 轮次1 | 轮次2 | 稳定性 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|---|---|---|---|

**D2. 逐笔请求的可复刻参数快照。**

对三段中**每一笔**请求，记录足以在本地复现的信息：完整 endpoint（host+path）、方法、Content-Type、
全部参数/字段名、请求头清单（尤其中签名类——可能由 native 层注入，Java 层看不到，见 E-31）、
body 形态（明文/加密；加密则转 protocol-signature-reverser）。

**D2.1 参数来源四分类（第四类不可省）**

| 来源 | 含义 | 本地实现要求 |
|---|---|---|
| **本地可生成** | 纯本地计算（时间戳/UUID/常量） | 直接构造 |
| **上一笔产出** | 来自前置接口的响应 | **必须引用前置结果**，形成数据依赖 |
| **服务端签发** | 服务端下发、须回带（令牌/凭证） | **必须从真实来源获取**，不可自造 |
| **`未知`** | **当前判不出来源** | **标未知 + 列入 D8 待验证**；❌ **禁止瞎归类** |

> **数据依赖链是重点**：把「A 接口的响应 → 成为 B 接口的入参」显式画出来。
> 这条链决定本地实现必须按什么顺序发请求，**不能跳步**。

**D2.2 D2 产出表**（每笔一张，须带证据字段）

| 参数 | 值/形态 | 来源（本地生成 / 上一笔产出 / 服务端签发 / 未知） | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|

**D3. 前置埋点 / 特征上报链路（强制，逐一回答）。**

目标接口能不能用，往往不取决于它本身，而取决于**配套请求有没有一起发生**（E-28）。
**必须逐项回答，任一项无证据即列入待验证清单，不得跳过**：

**D3.1 前置接口识别**——目标接口**之前**有哪些必须发生的请求？
- 典型：历史记录上报、页面曝光、前置详情、预热/预取、鉴权换票
- 判据：同操作**两轮复现**中恒定出现（判据同 D1.1）；并验证其产出是否被目标接口引用（数据依赖）

**D3.2 紧邻上报识别**——目标接口**之后立即**（同批/数秒内）发了什么？
- 典型：特征上报（`upload_ei_feature`）、批量埋点（`app_log`）、行为统计（`aweme/stats`）、监控
- 判据：与目标请求**成对出现**且共享会话时间窗

**D3.3 上报内容解析**——装的是什么、能不能读？
- 先查可读性：包头是否含明文密钥/IV、事件参数是否明文 → 可读则提取字段语义
- 不可读则记加密位置与归属，转 protocol-signature-reverser
- **读不到就写 `未知`**，不得凭接口名推测内容

**D3.4 上报通道梳理**——几个通道、各自职责、载荷量级、是否含设备指纹。

**D3.5 配对关系成表**（模板见 D5）。

**D3.6 因果验证**——对「缺配套请求 → 降级/拒绝」做**阻断实验**（见 D7）。

**D3.7 D3 产出表**（须带证据字段）

| 接口 | 角色（前置/紧邻上报/其它） | 与谁配对 | 载荷量级 | 可读性 | 配对强度 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|---|---|

**D4. ★ 本地复刻规格（本工作流的交付物）。**

把 D1–D3 转成**可直接实现的规格**：

**D4.1 访问序列（照抄执行顺序）**

```
step 1: <接口A>    参数来源: 本地生成        | 依赖: -
step 2: <接口B>    参数来源: A 响应字段 xxx  | 依赖: step1
step 3: <目标接口>  参数来源: 本地生成 + step1 的 xxx + 服务端签发 xxx
step 4: <配套上报>  参数来源: 本地生成        | 触发时机: 目标请求完成后立即
```

> 只把 D1 判定为**恒定/可变**的步骤写进主序列；**偶发**步骤单独列为"条件触发"。

**D4.2 每笔的复刻要点表**（须带证据字段）

| step | 接口 | 关键参数 | 参数来源 | 必须的凭证 | 头/签名要求 | 可否省略 | 省略后果 | 证据等级 | 置信度 |
|---|---|---|---|---|---|---|---|---|---|

> **「可否省略」/「省略后果」两列的填法**：有阻断实验 → 标「已实证」；无实验 → **只能标「推测」**
> 或写 `未知`（见 D7）。

**D4.3 节奏与顺序要求**：各步间隔量级（实测值）；哪些必须严格串行（有数据依赖的）；
上报是否必须落在同一会话窗口。**实测值须给证据路径**。

**D4.4 本地实现检查清单**（做完逐条打勾）
- [ ] 前置接口是否都发了，且其产出被正确引用
- [ ] 目标请求是否携带全部必填参数与凭证（含服务端签发的）
- [ ] 配套上报是否在目标请求后同批发出
- [ ] 参数完整性：可选参数是否也带上（"只带必要参数"本身即脚本指纹）
- [ ] 头清单是否与真实 App 一致（含 native 注入的签名类）
- [ ] 节奏是否符合实测间隔
- [ ] D2/D3 中标 `未知` 的项是否已列入 D8 验证清单

**D5. 配对关系表（前 / 中 / 后 三段联动，须带证据字段）。**

| 目标接口 | 前置（必须先发生） | 前置产出被谁用 | 紧邻（必须同时发生） | 回执（必须回带） | 配对强度 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|---|---|

**配对强度判据**：两轮复现均恒定成对 = 强；单轮出现或顺序可变 = 中；仅偶发 = 弱。

**D6. 降级判据（用于本地自检）。**

给出三级判据：真数据 / 软降级空壳 / 硬拒绝风控码（E-30）。
任何一级都不得仅凭 HTTP 200 或业务 code 判定成功（AGENTS.md 禁词纪律）。

**D6.1 降级判据本身也要有证据**（不可凭空定）

- **首选**：取**一次真实成功**与**一次实际被降级**的响应做**对照**，从中提取判别特征 → 标「已实证」
- 若只有成功样本、未见过降级 → 判据标「推测」，并列入 D8（设计触发降级的实验）
- **禁止**把"文档描述的空壳形态"当作本项目判据（来源不同平台不算实证）

**D6.2 D6 产出表**（须带证据字段）

| 级别 | 判别特征 | 判据来源 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|

**D7. 因果验证（强制，区别于相关性观测）。**

凡断言「A 导致 B」（如「缺埋点导致降级」「缺签名导致拒绝」「某步可省略」），
**必须设计阻断实验**：人为阻断 A（不发某笔 / 去掉某凭证），观测 B 是否出现。

- **做了阻断实验** → 证据等级可标「已实证」
- **仅有时序相关** → **证据等级只能标「推测」**，并列入 D8
- 实验设计写入 D8「待验证」章节

**D8. 产出与沉淀。**
- 复刻规格文档 → `projects/<target>/docs/flow-and-risk.md`，**必含**：
  三段时序表（前/中/后，带证据等级）/ 逐笔参数快照（带来源四分类）/ **数据依赖链** /
  D3 六问全答 / **D4 本地复刻规格（序列 + 要点表 + 检查清单）** / 配对关系表 /
  降级判据（带判据来源）/ 待验证实验清单
- 接口维度登记 → 更新 `references/projects/<target>.md` 的「接口风控速查表」
- 可泛化角度 → 按 A5 沉淀 L1

**产出验收标准**（三条全满足才算完成）：
1. **可实施**：别人拿着这份文档能独立把本地访问实现出来，无需回头问"这里该发什么"
2. **可追溯**：每条结论都有证据等级 + 置信度 + 原始证据路径
3. **无幻觉**：判不出来的写 `未知` 并进 D8，不以推测充数

"""

t2 = t[:iD] + NEW_D + t[iExt:]
open(SK, "w", encoding="utf-8").write(t2)

# ============ B. templates.md §2.5 ============
tp = open(TP, encoding="utf-8").read()
i = tp.find("## §2.5")
j = tp.find("## §2 项目风控记录模板")
assert 0 < i < j

NEW_25 = r"""## §2.5 单接口访问流程复刻模板（SKILL.md 工作流 D）

写入路径：`projects/<target>/docs/flow-and-risk.md`

**用途**：把真实 App 访问某接口的「前 / 中 / 后」完整记录下来，产出**可在本地照着重演的访问规格**，
用于在本地实现该接口访问而**不触发风控**。

**适用范围**：服务**离线纯协议实现**；RPC 桥接只需取其中**降级判据 + 字段谱系**。

**验收标准**（三条全满足）：① 别人能据此独立实现本地访问 ② 每条结论有证据等级+置信度+证据路径
③ 判不出的写 `未知` 并进待验证清单。

```markdown
# <目标> 接口访问流程复刻规格

> 生成日期: YYYY-MM-DD | 平台: | 涉及接口: IF-xx… | 目标：本地复刻该访问流程
> 适用范围: 离线纯协议实现（RPC 桥接只需取降级判据+字段谱系）
> 资料基线: <抓包/hook 证据路径>

## 〇、证据纪律

**证据来源优先级**：官方文档/协议规范 ＞ 本项目实测抓包/hook ＞ 技术社区 ＞ 自媒体

**证据等级**（阈值见 §3.1）：已实证 / 大样本 / 小样本 / 推测 / 未知
**置信度**：高 / 中 / 低　　**样本量**：<账号数>账号 × <IP/设备数> × <复现次数>

> 规则：证据不足写「未知」；断言「A 导致 B」必须有阻断实验，否则最高标「推测」。

## 一、完整时序（前 / 中 / 后 三段，复现 ≥2 轮）

| 段 | 序 | 接口 | 方法 | Host | 轮次1 | 轮次2 | 稳定性 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|---|---|---|---|
| **前** | 1 | /api/前置 | POST | ... | ✓ | ✓ | 恒定 | 已实证 | 高 | capture/xxx |
| **中** | 2 | /api/目标 | POST | ... | ✓ | ✓ | 恒定 | 已实证 | 高 | capture/xxx |
| **后** | 3 | /api/上报 | POST | ... | ✓ | ✓ | 恒定 | 已实证 | 高 | capture/xxx |

> **稳定性判据**：恒定=≥2 轮均出现且相对位置一致；可变=均出现但顺序不固定；偶发=仅部分轮次出现。
> **不一致处理**：两轮冲突→追加第 3 轮；仍不稳定标「偶发」+ 注明条件。
> **仅 1 轮** → 全部标「小样本」，不得判「恒定」，待验证必须列"补跑第 2 轮"。

## 二、逐笔请求的可复刻参数快照

### step 1 — <前置接口>
- endpoint / 方法 / Content-Type：
- 参数清单：

| 参数 | 值/形态 | 来源 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|
| xxx | ... | 本地生成 / 上一笔产出 / 服务端签发 / **未知** | | | |

- 请求头（含签名类，注明是否 native 注入）：
- body 形态（明文/加密）：

### step 2 — <目标接口>
（同上结构）

### step 3 — <配套上报>
（同上结构）

> **来源四分类**：本地可生成 / 上一笔产出 / 服务端签发 / **未知**（判不出就写未知，禁止瞎归类）。

## 三、★ 数据依赖链（决定本地发请求的顺序）

```
本地生成 ─┐
          ├─→ step1 <前置>  响应字段 xxx ─┐
          │                                ├─→ step3 <目标>
服务端签发 xxx ───────────────────────────┘
                                    ↓
                              step4 <配套上报>（同批）
```

> 有依赖的步骤**必须串行**，不能跳步、不能并发。

## 四、前置埋点 / 特征上报链路（D3 六问全答）

**4.1 前置接口**

| 接口 | 作用 | 出现轮次 | 必须性 | 产出被谁用 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|---|

**4.2 紧邻上报**

| 上报接口 | 与哪笔配对 | 延迟量级 | 配对强度 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|

**4.3 上报内容**

| 上报接口 | 载荷量级 | 事件数 | 可读性 | 关键字段 | 加密归属 | 证据等级 | 置信度 |
|---|---|---|---|---|---|---|---|

**4.4 上报通道**

| 通道 | 接口 | 职责 | 是否含指纹 | 证据等级 | 置信度 |
|---|---|---|---|---|---|

## 五、★ 本地复刻规格

### 5.1 访问序列（照抄执行顺序，仅含恒定/可变步骤）
```
step 1: <接口A>    参数来源: 本地生成        | 依赖: -
step 2: <接口B>    参数来源: A 响应字段 xxx  | 依赖: step1
step 3: <目标接口>  参数来源: 本地生成 + step1 的 xxx + 服务端签发 xxx
step 4: <配套上报>  参数来源: 本地生成        | 触发时机: 目标请求完成后立即
```
> 偶发步骤单独列为"条件触发"，不进主序列。

### 5.2 每笔复刻要点表

| step | 接口 | 关键参数 | 参数来源 | 必须的凭证 | 头/签名要求 | 可否省略 | 省略后果 | 证据等级 | 置信度 |
|---|---|---|---|---|---|---|---|---|---|

> 「可否省略 / 省略后果」：有阻断实验→已实证；无实验→只能标「推测」或写 `未知`。

### 5.3 节奏与顺序要求
- 各步间隔量级（实测值 + 证据路径）：
- 必须严格串行的步骤：
- 上报是否须落在同一会话窗口：

### 5.4 本地实现检查清单（做完逐条打勾）
- [ ] 前置接口都发了，且其产出被正确引用
- [ ] 目标请求携带全部必填参数与凭证（含服务端签发的）
- [ ] 配套上报在目标请求后同批发出
- [ ] 参数完整性：可选参数也带上（"只带必要参数"本身即脚本指纹）
- [ ] 头清单与真实 App 一致（含 native 注入的签名类）
- [ ] 节奏符合实测间隔
- [ ] 所有标 `未知` 的项已列入待验证清单

## 六、配对关系表（三段联动）

| 目标接口 | 前置（必须先发生） | 前置产出被谁用 | 紧邻（必须同时发生） | 回执（必须回带） | 配对强度 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|---|---|---|

## 七、降级判据（本地自检用）

| 级别 | 判别特征 | 判据来源 | 证据等级 | 置信度 | 证据 |
|---|---|---|---|---|---|
| OK | 含业务字段 | | | | |
| 软降级 | 空壳（HTTP200 + 业务码正常但列表空） | | | | |
| 硬拒绝 | 风控码（如 -99999） | | | | |

> **判据来源要求**：首选「一次真实成功 + 一次实际被降级」的对照；未见降级样本时，判据标「推测」
> 并列入待验证。禁止把其他平台文档描述当作本项目判据。
> 三级均不得仅凭 HTTP 200 或业务 code 判成功。

## 八、待验证实验清单

| 编号 | 假设 | 实验设计（阻断实验） | 优先级 |
|---|---|---|---|
| V-1 | 缺 <某笔> 致降级 | 阻断该笔，观察目标响应 | 高 |
```

"""

tp2 = tp[:i] + NEW_25 + tp[j:]
open(TP, "w", encoding="utf-8").write(tp2)

# ============ C. 校验 ============
chk = open(SK, encoding="utf-8").read()
heads = [m.group(1) for m in re.finditer(r"^## 工作流 ([A-Z])：", chk, re.M)]
d = chk[chk.find("## 工作流 D"):chk.find("## 扩展协议")]
ids = re.findall(r"^\*\*(D\d(?:\.\d)?)[\. ]", d, re.M)

print("SKILL 工作流:", heads, "| 无重复:", heads == ["A", "B", "C", "D"])
print("D 步骤编号:", ids)
print("  唯一:", len(ids) == len(set(ids)))
print()
print("=== D 新增补强点 ===")
for k in ["D0.1 证据来源优先级", "D0.2 每个结论都要带证据等级", "D0.3 因果论断的证据门槛",
          "D0.4 每一笔记录都要有原始证据路径", "D1.1 稳定性判据", "D1.2 轮次不一致时的处理",
          "D2.1 参数来源四分类", "D6.1 降级判据本身也要有证据", "产出验收标准"]:
    print(f"  {k:32s}: {k in d}")

print()
print("=== 表格是否都带证据列 ===")
tabs = re.findall(r"\|[^\n]*\|[^\n]*\n\|[-| ]+\|", d)
evn = sum(1 for x in tabs if "证据等级" in x)
print(f"  D 内表格数: {len(tabs)}，含'证据等级'列: {evn}")

t2s = open(TP, encoding="utf-8").read()
print()
print("=== templates §2.5 ===")
print("  标题:", [l for l in t2s.split('\n') if l.startswith('## §2.5')][0][:52])
s25 = t2s[t2s.find("## §2.5"):t2s.find("## §2 项目风控记录模板")]
print("  含证据纪律:", "证据纪律" in s25)
print("  含来源四分类:", "来源四分类" in s25)
print("  含稳定性判据:", "稳定性判据" in s25)
print("  含判据来源要求:", "判据来源要求" in s25)
print("  表格含证据等级列数:", s25.count("证据等级"))
print()
print("SKILL 行数:", chk.count("\n")+1, "| templates 行数:", t2s.count("\n")+1)
