# -*- coding: utf-8 -*-
"""向 risk-control-adversary 的 L1 知识库追加 dy 项目衍生条目
   E-28~E-32（5 条）+ F-06~F-07（2 条），并同步索引表
"""
import re, os

P = r"D:\reserve_agent\android\.dsh\skills\risk-control-adversary\references\general-principles.md"
txt = open(P, encoding="utf-8").read()
os.makedirs(os.path.dirname(P) + r"\_bak", exist_ok=True)
open(P + ".bak-dy", "w", encoding="utf-8").write(txt)

# ---------- 1) 索引表新增行 ----------
idx_rows = [
    "| E-28 ⭐ | 请求前置链路配对校验 | 请求策略与行为画像 | 已实证 | 高 | v1.0 | 生效 | 请求策略与行为画像/Android/进阶/埋点/前置链路 | projects/dy 2026-09-10 |",
    "| E-29 | 服务端签发会话凭证的串联回带 | 请求策略与行为画像 | 已实证 | 高 | v1.0 | 生效 | 请求策略与行为画像/Android/进阶/会话凭证/串联 | projects/dy 2026-09-10 |",
    "| E-30 | 降级分级识别（软降级 vs 硬拒绝） | 请求策略与行为画像 | 已实证 | 高 | v1.0 | 生效 | 请求策略与行为画像/跨平台/进阶/降级/止损 | projects/dy 2026-09-10 |",
    "| E-31 ⭐ | 风控签名位于 native 层（跨层观测不可互否） | 签名 | 已实证 | 高 | v1.0 | 生效 | 签名/Android/进阶/native/跨层 | projects/dy 2026-09-10 |",
    "| E-32 ⭐ | 单接口全链路风控分析（七层链路法） | 请求策略与行为画像 | 已实证 | 高 | v1.0 | 生效 | 请求策略与行为画像/跨平台/进阶/方法论/链路分析 | projects/dy 2026-09-10 |",
    "| F-06 ⭐ | 协议直发缺 native 签名头致硬拒绝 | 失败经验 | 已实证 | 高 | v1.0 | 生效 | 签名/Android/进阶/直发/风控码 | projects/dy 2026-09-10 |",
    "| F-07 | 高频 hook 点与多脚本加载致进程崩溃 | 失败经验 | 已实证 | 高 | v1.0 | 生效 | 环境检测/Android/入门/Frida/稳定性 | projects/dy 2026-09-10 |",
]

# 找索引表最后一行（以 | 开头且含日期），插入其后
lines = txt.split("\n")
last_idx = -1
for i, l in enumerate(lines):
    if l.startswith("| ") and ("2026-" in l or "20" in l) and l.count("|") >= 9:
        # 确认是索引表区域（在 ## 条目索引 与 ## 条目 之间）
        pass
idx_start = txt.find("## 条目索引")
idx_end = txt.find("## 条目", idx_start + 5)
block = txt[idx_start:idx_end]
blines = block.split("\n")
ins = len(blines)
for i in range(len(blines) - 1, -1, -1):
    if blines[i].startswith("| ") and blines[i].count("|") >= 9:
        ins = i + 1
        break
blines = blines[:ins] + idx_rows + blines[ins:]
txt = txt[:idx_start] + "\n".join(blines) + txt[idx_end:]

# ---------- 2) 正文追加条目 ----------
entries = r"""

### E-28 请求前置链路配对校验

- 类型: 请求策略与行为画像（+业务规则风控）
- 检测原理: 风控不看单个业务请求，而校验「业务请求 + 紧邻的特征上报」是否成对出现；缺失或错序即视为脚本流量
- 对抗思路: 复刻完整前置链路而非只打目标接口——按实测时序先发前置接口（历史/行为记录），业务请求后紧跟特征上报接口，保持与真实 App 一致的批次构成
- 落地工具: 本 skill 策略 → android-recon 抓包链路还原 + 本项目 `scripts/risk_health.py`（配对率统计）
- 适用平台: 跨平台
- 适用范围: 有独立特征上报接口的 App（埋点/特征通道与业务接口并存）；无独立上报通道的场景不适用
- 操作边界: 不伪造上报内容（只保证链路完整）；配对率 <1.0 即停并归因
- 异常判定: 业务请求成功但数据为空壳、或出现风控码 = 配对缺失信号
- 关键步骤: 1. 抓包还原接口时序 2. 统计「业务请求↔上报」配对率 3. 复刻时序重放
- 平台适配: Android 用 Frida hook RequestBuilder；Web 用浏览器网络面板按 initiator 链还原
- 证据等级: 已实证（样本量: 1 设备 × 3 链路 × 3 复现）
- 置信度: 高（依据: 抖音实测时序 + 两轮复现同序）
- 失效状态: 生效
- 版本: v1.0
- 标签: 请求策略与行为画像/Android/进阶/埋点/前置链路
- 关联: E-12（埋点链路补齐，本条为其量化校验补强）；F-06（缺配对致拒绝）
- 来源: projects/dy 2026-09-10

### E-29 服务端签发会话凭证的串联回带

- 类型: 请求策略与行为画像
- 检测原理: 服务端在响应中下发会话凭证（如 search_id / authentication_token），后续请求必须回带该凭证，服务端据此串联可信会话链；凭证由服务端生成，客户端无法伪造
- 对抗思路: 不自行编造会话标识，而是「首次请求取凭证 → 后续请求原样回带」；凭证与业务参数（count/author 等）须一致
- 落地工具: 本 skill 策略 → android-recon 抓包提取凭证 → 项目脚本提取并回带（本项目 `scripts/risk_health.py` 记录签发）
- 适用平台: 跨平台
- 适用范围: 响应体含会话凭证字段的接口；纯无状态接口不适用
- 操作边界: 不跨账号复用凭证；凭证过期即重新获取，不延长使用
- 异常判定: 回带错误/过期凭证 → 空数据或风控码
- 关键步骤: 1. 定位响应中的凭证字段 2. 记录签发规律（每次/每会话） 3. 后续请求回带并校验一致性
- 平台适配: -
- 证据等级: 已实证（样本量: 1 设备 × 5 次搜索 × 2 链路）
- 置信度: 高（依据: 抖音响应字段实测 + 格式吻合）
- 失效状态: 生效
- 版本: v1.0
- 标签: 请求策略与行为画像/Android/进阶/会话凭证/串联
- 关联: E-28（链路配对的凭证维度）；E-30（凭证失效后的降级表现）
- 来源: projects/dy 2026-09-10

### E-30 降级分级识别（软降级 vs 硬拒绝）

- 类型: 请求策略与行为画像（+业务规则风控）
- 检测原理: 风控对不同风险等级采取不同响应：低风险「软降级」返回结构合法但内容为空的响应（HTTP 200、业务码正常），高风险「硬拒绝」返回专用风控码；两者都极易被误判为成功
- 对抗思路: 建立三级判官——真数据（含业务字段）/ 软降级（空壳特征）/ 硬拒绝（风控码字符串）；任何一级不得仅凭 HTTP 200 或业务 code 判定成功
- 落地工具: 本 skill 策略 → 项目脚本判官函数（本项目 `scripts/risk_health.py::judge_response`）
- 适用平台: 跨平台
- 适用范围: 所有返回结构化数据的接口；文件下载类需改用字节校验
- 操作边界: 判定为软降级即停止同类请求并归因，不重试刷量
- 异常判定: 出现空壳（如 has_more=0 且列表空）或风控码（如 -99999）→ 立即熔断
- 关键步骤: 1. 采集一次真实成功样本作基线 2. 提取空壳特征指纹 3. 在采集脚本中前置判官
- 平台适配: -
- 证据等级: 已实证（样本量: 1 设备 × 2 链路 × 3 复现）
- 置信度: 高（依据: 抖音软降级/硬拒绝双实测 + 对照实验）
- 失效状态: 生效
- 版本: v1.0
- 标签: 请求策略与行为画像/跨平台/进阶/降级/止损
- 关联: E-27（柔性降级伪装识别，本条给出两级量化判据）；E-28（降级的常见诱因）
- 来源: projects/dy 2026-09-10

### E-31 风控签名位于 native 层（跨层观测不可互否）

- 类型: 签名
- 检测原理: 强签名平台的签名头由 native 层（SO）在发送前注入传输流，Java 层完全不可见；因此在 Java 层 hook 签名相关方法会得到「零调用」的假象
- 对抗思路: 以**传输层实际字节**为唯一权威证据——抓 TLS 请求头（HTTP/2 需 HPACK+Huffman 解码）或抓 native 发送函数，不依赖单一层次的 hook 命中
- 落地工具: 本项目 `scripts/cap_h2_headers.py`（SSL_write → HTTP/2 帧 → HPACK → Huffman 全解码）；签名算法还原交 protocol-signature-reverser
- 适用平台: Android
- 适用范围: 存在 native 签名 SDK 的 App；纯 Java 签名的场景不适用
- 操作边界: 不因单层零命中就断言机制不存在；跨层结论冲突时以传输层字节为准
- 异常判定: Java 层 hook 零命中但请求头存在签名 → 即为本条适用信号
- 关键步骤: 1. hook 传输层取原始请求头 2. 解码协议封装（H2/TLS） 3. 与 Java 层观测交叉验证
- 平台适配: iOS 对应 hook NSURLSession/SSL 层；Web 用浏览器网络面板
- 证据等级: 已实证（样本量: 1 设备 × 1 App × 多次复现）
- 置信度: 高（依据: 抖音实测，Java 层 0 命中 vs 传输层抓到 x-argus/x-gorgon/x-ladon）
- 失效状态: 生效
- 版本: v1.0
- 标签: 签名/Android/进阶/native/跨层
- 关联: F-06（跨层误判的失败教训）；E-22（传输层指纹同属跨层观测）
- 来源: projects/dy 2026-09-10

### E-32 单接口全链路风控分析（七层链路法）

- 类型: 请求策略与行为画像（+其他）
- 检测原理: 单个接口的风险不由该接口自身决定，而由其在链路中的位置与配套行为决定；孤立看接口必然漏判
- 对抗思路: 对每个目标接口按固定七层做全链路分析——① 触发层 ② 参数层 ③ **前置特征层**（上报/行为链） ④ 签名层 ⑤ 传输层 ⑥ 响应层 ⑦ 回执层；重点标注「前置」与「回执」两端的配对关系
- 落地工具: 本 skill 工作流 D（单接口全链路风控分析）；产出模板见 templates.md（含前置链路表与风控点表）
- 适用平台: 跨平台
- 适用范围: 任何需要评估采集可行性的目标接口
- 操作边界: 七层未走完不得给出「接口已通」结论；标注未知层并附验证实验
- 异常判定: 七层中任一层缺证据 → 该层结论降级为「推测」并列入验证清单
- 关键步骤: 1. 抓包还原完整请求序列 2. 按七层归类证据 3. 标注每层的证据等级 4. 输出前置链路表
- 平台适配: -
- 证据等级: 已实证（样本量: 1 项目 × 3 条链路全流程）
- 置信度: 高（依据: 抖音三链路（搜索/视频/评论）全链路实测）
- 失效状态: 生效
- 版本: v1.0
- 标签: 请求策略与行为画像/跨平台/进阶/方法论/链路分析
- 关联: E-28（第三层细化）；E-29（第七层细化）；E-30（第六层细化）
- 来源: projects/dy 2026-09-10

### F-06 协议直发缺 native 签名头致硬拒绝

- 类型: 失败经验
- 触发条件: 用自己的 HTTP 客户端直发业务请求（URL/body 逐字复用真实抓包），但未携带 native 层注入的签名头
- 失败特征: `status_code: -99999`（硬拒绝）或空壳响应；补齐签名头后风控码消失
- 根因分析: 签名头由 native 层在发送前注入 TLS 流，重放 URL+body 无法覆盖该层；且 Java 层 hook 观测不到，容易误判为「无需签名」
- 规避方案: 直发前必须先抓齐**传输层真实请求头**（含签名 + Cookie 全量）；或改用 RPC 桥接让 App 自发请求
- 影响等级: 严重（阻断协议直发路径）
- 适用风控版本: 抖音 38.0.0（八神 + TTNet）
- 失效条件: 目标平台改为纯 Java 层签名时
- 落地工具: `scripts/cap_h2_headers.py` 抓头 + `scripts/risk_health.py` 判官
- 适用平台: Android
- 证据等级: 已实证（样本量: 1 设备 × 1 接口 × 2 对照组）
- 置信度: 高（依据: 抖音直发对照实验）
- 失效状态: 生效
- 版本: v1.0
- 标签: 签名/Android/进阶/直发/风控码
- 关联: E-31（同源的机制解释）；E-30（硬拒绝判据）
- 转化: 候选（待跨项目复现）
- 来源: projects/dy 2026-09-10

### F-07 高频 hook 点与多脚本加载致进程崩溃

- 类型: 失败经验
- 触发条件: ① hook 高频调用的集合方法（如 HashMap.put）并在回调内构造 Java 对象；② 向同一进程加载多个 Frida 脚本
- 失败特征: ① App SIGSEGV（`JNI DETECTED ERROR: java_string == null`），backtrace 显示与 App 自身 hook 框架冲突；② `script has been destroyed`
- 根因分析: ① 热路径 + JNI 调用开销 + 与 App 自身字节码 hook 框架竞争；② Frida 单进程多脚本生命周期互相回收
- 规避方案: ① 只在**低频**时机 hook（构造函数/请求构建），回调内只做字符串拼接不做对象构造；② 多能力合并为**单脚本**，用 rpc.exports 多功能导出
- 影响等级: 一般（可恢复，但会打断采集节奏）
- 适用风控版本: -
- 失效条件: -
- 落地工具: 本项目 `hooks/` 下单脚本多导出模式
- 适用平台: Android
- 证据等级: 已实证（样本量: 1 设备 × 2 场景 × 多次）
- 置信度: 高（依据: 抖音实测崩溃 + 修复验证）
- 失效状态: 生效
- 版本: v1.0
- 标签: 环境检测/Android/入门/Frida/稳定性
- 关联: E-01（进程稳定是隐蔽性前提）
- 转化: 候选（待跨项目复现）
- 来源: projects/dy 2026-09-10
"""

# 追加到文件末尾
if not txt.endswith("\n"):
    txt += "\n"
txt += entries

open(P, "w", encoding="utf-8").write(txt)

# 校验
chk = open(P, encoding="utf-8").read()
print("E- 条目总数:", len(re.findall(r"^### E-\d+", chk, re.M)))
print("F- 条目总数:", len(re.findall(r"^### F-\d+", chk, re.M)))
print("索引行总数:", len([l for l in chk.split("\n") if re.match(r"^\| [EF]-\d+", l)]))
print("E-28..E-32 存在:", all(f"### E-{n}" in chk for n in range(28, 33)))
print("F-06..F-07 存在:", all(f"### F-0{n}" in chk for n in (6, 7)))
